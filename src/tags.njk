---
layout: default
title: Tag List
permalink: "tags/index.html"
---

<h1 class="text-3xl text-black dark:text-white leading-normal font-bold mt-0 mb-7 text-center">{{ title }}</h1>
<div class="flex justify-center flex-wrap">
    {% for name, length in collections.tagList %}
    <a href="{{ '/tags/' | url }}{{ name }}" class="inline-block mx-1 mb-3 px-4 py-2 bg-neutral-600 text-white font-medium rounded-full">
        {{ name | capitalize }}
        <span class="px-2 py-1 ml-1 text-xs font-semibold bg-black text-white rounded-full">{{ length }}</span>
    </a>
    {% endfor %}
</div>